<header class="site-header">
	<nav>
		<a href="/" class="active">Home</a>
		<a href="/commissions">Commissions</a>
		<a href="/merch">Merch</a>
		<a href="/gallery">Gallery</a>
		<a href="/terms">Terms</a>
	</nav>
</header>

<style>
	.site-header {
		width: 100%;
		padding: 2rem 1.5rem 0;
	}

	nav {
		max-width: 900px;
		margin: 0 auto;

		display: flex;
		justify-content: center;
		align-items: center;

		gap: 2.5rem;

		padding: 1rem 2rem;

		border: 1px solid rgba(255, 112, 209, 0.55);
		border-radius: 999px;

		background: rgba(120, 20, 100, 0.18);

		box-shadow:
			0 0 15px rgba(255, 80, 200, 0.15),
			inset 0 0 20px rgba(255, 80, 200, 0.05);

		backdrop-filter: blur(10px);
	}

	nav a {
		position: relative;

		color: rgba(255, 255, 255, 0.65);

		font-size: 1rem;
		font-weight: 500;

		transition:
			color 0.2s ease,
			text-shadow 0.2s ease;
	}

	nav a:hover,
	nav a.active {
		color: white;

		text-shadow:
			0 0 10px rgba(255, 100, 220, 0.8);
	}

	nav a.active::after {
		content: "";

		position: absolute;

		left: 50%;
		bottom: -0.65rem;

		width: 5px;
		height: 5px;

		transform: translateX(-50%);

		border-radius: 50%;

		background: var(--pink-light);

		box-shadow:
			0 0 8px var(--pink-light);
	}

	@media (max-width: 700px) {
		.site-header {
			padding: 1rem 0.75rem 0;
		}

		nav {
			gap: 1rem;

			padding: 0.8rem 1rem;
		}

		nav a {
			font-size: 0.8rem;
		}
	}

	@media (max-width: 480px) {
		nav {
			gap: 0.65rem;
		}

		nav a {
			font-size: 0.72rem;
		}
	}
</style>
